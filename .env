# Workspace Genie Agent Configuration
# This file contains local environment variables that won't be committed to git

# =============================================================================
# WORKSPACE CONFIGURATION
# =============================================================================
# Which workspace to work on by default, this is the also the index name
WORKSPACE=

# =============================================================================
# LLM CONFIGURATION  
# =============================================================================
# Which LLM model to use (options: ollama, ollama-coder, ollama-large, claude)
LLM_MODEL=ollama

# =============================================================================
# DEBUG CONFIGURATION
# =============================================================================
# Enable detailed debug logging (true/false)
DEBUG_AGENT=false

# =============================================================================
# SERVICE CONFIGURATION
# =============================================================================
# Ollama server URL (default: http://localhost:11434)
OLLAMA_BASE_URL=http://localhost:11434

# Qdrant server URL (default: http://localhost:6333)
QDRANT_URL=http://localhost:6333

# =============================================================================
# USAGE EXAMPLES
# =============================================================================
# To use a specific workspace:
#   export WORKSPACE=my-project && mise run agent
#
# To enable debug logging:
#   export DEBUG_AGENT=true && mise run agent
#
# To use different LLM:
#   export LLM_MODEL=claude && mise run agent
#
# To start required services:
#   ollama serve &                    # Start Ollama
#   mise run qdrant-start              # Start Qdrant
#   mise run index /path/to/workspace   # Index workspace
